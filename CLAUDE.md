See [AGENTS.md](AGENTS.md).

That file is the single source of truth for how to work in this repository, and it is deliberately
tool-agnostic. This pointer exists so Claude Code finds it too — duplicating the content here would
guarantee the two copies drift, and a stale rule is worse than no rule.
